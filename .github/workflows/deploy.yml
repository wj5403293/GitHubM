# GitHub Pages 自动部署 + Android APK 构建 + Release 发布工作流
# 推送到 main 分支时：
#   1. 构建 Web 应用（dist/）
#   2. 部署到 GitHub Pages
#   3. 打包签名 Release APK（WebView 壳 + dist/ 资源）
#   4. 自动创建 GitHub Release 并上传 APK
#
# ── 首次使用前的 Secrets 配置 ────────────────────────────────────────
# 在仓库 Settings → Secrets and variables → Actions 中添加：
#
#   RELEASE_KEYSTORE_BASE64   release.keystore 文件的 base64 内容
#   RELEASE_KEY_ALIAS         密钥别名（默认 github-manager）
#   RELEASE_STORE_PASSWORD    keystore 密码
#   RELEASE_KEY_PASSWORD      key 密码
#
# 运行 android/generate-keystore.sh 脚本可一键生成并输出上述值。
# 未配置 Secrets 时，CI 会改用 debug 签名构建（可在同设备覆盖安装）。
# ────────────────────────────────────────────────────────────────────
name: Deploy & Build APK

on:
  push:
    branches: [main]
  workflow_dispatch: # 支持手动触发

# GITHUB_TOKEN 权限
permissions:
  contents: write   # 创建 GitHub Release 需要 write 权限
  pages: write
  id-token: write

# 同组并发：新部署取消旧部署（仅影响 Pages，APK 构建不受影响）
concurrency:
  group: pages
  cancel-in-progress: true

# ─────────────────────────────────────────────────────────────────
jobs:

  # ── Job 1：构建 Web 应用 ───────────────────────────────────────
  build-web:
    name: 构建 Web 应用
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: 构建 Web 应用（注入版本号）
        env:
          # 版本号格式 1.0.<run_number>，与 APK versionName 保持一致
          VITE_APP_VERSION: "1.0.${{ github.run_number }}"
        run: pnpm exec vite build

      - name: 上传 Web 构建产物（供后续 Job 使用）
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: dist
          retention-days: 1

      - name: 配置 GitHub Pages
        uses: actions/configure-pages@v5

      - name: 上传 Pages 产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  # ── Job 2：部署到 GitHub Pages ─────────────────────────────────
  deploy-pages:
    name: 部署到 GitHub Pages
    needs: build-web
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # ── Job 3：构建签名 Release APK 并发布 ────────────────────────
  build-apk:
    name: 构建 Android APK 并发布 Release
    needs: build-web
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码（含 android/ 项目源码）
        uses: actions/checkout@v4

      - name: 下载 Web 构建产物
        uses: actions/download-artifact@v4
        with:
          name: web-dist
          path: dist

      - name: 将 Web 资源复制到 Android assets
        run: |
          mkdir -p android/app/src/main/assets
          cp -r dist/. android/app/src/main/assets/

      - name: 设置 JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 设置 Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'
          cache-read-only: false

      - name: 生成 local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > android/local.properties

      - name: 接受 Android SDK 许可证
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses 2>/dev/null || true

      # ── 签名配置：优先使用 Release Keystore Secret ───────────────
      - name: 配置 Release Keystore（Secret 存在时）
        id: keystore
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
        run: |
          if [ -n "${RELEASE_KEYSTORE_BASE64}" ]; then
            echo "使用 Release Keystore 签名"
            KEYSTORE_PATH="${GITHUB_WORKSPACE}/android/release.keystore"
            echo "${RELEASE_KEYSTORE_BASE64}" | base64 --decode > "${KEYSTORE_PATH}"
            echo "keystore_path=${KEYSTORE_PATH}" >> "$GITHUB_OUTPUT"
            echo "signing_mode=release" >> "$GITHUB_OUTPUT"
          else
            echo "⚠️ 未配置 RELEASE_KEYSTORE_BASE64 Secret，将使用 Debug 签名（适合内部测试）"
            echo "   如需分发给用户，请运行 android/generate-keystore.sh 并配置 Secrets"
            echo "keystore_path=" >> "$GITHUB_OUTPUT"
            echo "signing_mode=debug" >> "$GITHUB_OUTPUT"
          fi

      # ── 构建 APK（Release / Debug 签名自动选择）───────────────────
      - name: 构建 APK
        working-directory: android
        env:
          VERSION_CODE: ${{ github.run_number }}
          VERSION_NAME: "1.0.${{ github.run_number }}"
          KEYSTORE_PATH: ${{ steps.keystore.outputs.keystore_path }}
          STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          if [ "${{ steps.keystore.outputs.signing_mode }}" = "release" ]; then
            gradle assembleRelease --no-daemon
          else
            gradle assembleDebug --no-daemon
          fi

      # ── 规范化 APK 文件名 ──────────────────────────────────────
      - name: 重命名 APK
        id: rename_apk
        run: |
          VERSION_NAME="1.0.${{ github.run_number }}"
          APK_NAME="github-manager-v${VERSION_NAME}.apk"
          SIGNING_MODE="${{ steps.keystore.outputs.signing_mode }}"
          if [ "${SIGNING_MODE}" = "release" ]; then
            SRC="android/app/build/outputs/apk/release/app-release.apk"
          else
            SRC="android/app/build/outputs/apk/debug/app-debug.apk"
          fi
          cp "${SRC}" "${APK_NAME}"
          echo "apk_name=${APK_NAME}" >> "$GITHUB_OUTPUT"
          echo "version_name=${VERSION_NAME}" >> "$GITHUB_OUTPUT"

      # ── 创建 GitHub Release 并上传 APK ────────────────────────
      - name: 创建 GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION_NAME="${{ steps.rename_apk.outputs.version_name }}"
          APK_NAME="${{ steps.rename_apk.outputs.apk_name }}"
          SIGNING_MODE="${{ steps.keystore.outputs.signing_mode }}"
          TAG="v${VERSION_NAME}"
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA="${COMMIT_SHA:0:7}"

          # 生成 Release Notes
          if [ "${SIGNING_MODE}" = "release" ]; then
            SIGN_NOTE="✅ **Release 签名**（可覆盖安装，正式分发）"
          else
            SIGN_NOTE="⚠️ **Debug 签名**（仅供内部测试，配置 Secrets 后将自动升级为 Release 签名）"
          fi

          NOTES="## GitHub Manager ${TAG}

          **构建信息**
          - 版本号：\`${VERSION_NAME}\`（versionCode: \`${{ github.run_number }}\`）
          - 提交：[\`${SHORT_SHA}\`](${{ github.server_url }}/${{ github.repository }}/commit/${COMMIT_SHA})
          - 构建时间：$(date -u '+%Y-%m-%d %H:%M UTC')
          - ${SIGN_NOTE}

          **安装方式**
          1. 下载下方 APK 文件到 Android 设备
          2. 允许「安装未知来源应用」后点击安装
          3. 如已安装旧版本，使用 Release 签名版本可直接覆盖安装（无需卸载）

          **系统要求**
          - Android 8.0（API 26）及以上"

          # 如同名 tag 已存在则先删除（workflow_dispatch 重复触发场景）
          gh release delete "${TAG}" --yes 2>/dev/null || true
          git push origin --delete "${TAG}" 2>/dev/null || true

          gh release create "${TAG}" \
            --title "GitHub Manager ${TAG}" \
            --notes "${NOTES}" \
            --latest \
            "${APK_NAME}"

